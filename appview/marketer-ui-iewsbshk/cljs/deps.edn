{:paths ["src"]
 :deps {org.clojure/clojure {:mvn/version "1.12.0"}
        io.github.kotoba-lang/jp-go-digital-design-system
        {:git/url "https://github.com/kotoba-lang/jp-go-digital-design-system.git"
         :git/sha "dc4602dc17bce8f6e9ba038e92b3e95fcc411b49"}}
 ;; reagent / re-frame / clojurescript / shadow-cljs live under the :cljs
 ;; alias (not top-level :deps) per ADR-2608201300 (`jvm-new-surface-guard`
 ;; hook, cutover :new-jvm-runtime-dependencies :forbidden) — new top-level
 ;; third-party maven runtime coords are refused. shadow-cljs.edn's
 ;; `:deps {:aliases [:cljs :test]}` merges this alias for every build, so
 ;; both the :app and :test targets still see these on the classpath.
 :aliases {:cljs {:extra-deps {org.clojure/clojurescript {:mvn/version "1.11.132"}
                                reagent/reagent {:mvn/version "1.2.0"}
                                re-frame/re-frame {:mvn/version "1.4.3"}
                                thheller/shadow-cljs {:mvn/version "2.28.20"}}}
           :test {:extra-paths ["test"]}}}
